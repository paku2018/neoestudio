<!DOCTYPE html>
<html>
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSRF Token -->
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.0.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    <!-- Bootstrap CSS -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>👮</text></svg>">

    <!-- Google Tag Manager - SK 210422 -->
    <script>(function (w, d, s, l, i) {
            w[l] = w[l] || [];
            w[l].push({
                'gtm.start': new Date().getTime(), event: 'gtm.js'
            });
            var f = d.getElementsByTagName(s)[0],
                j = d.createElement(s), dl = l != 'dataLayer' ? '&l=' + l : '';
            j.async = true;
            j.src =
                'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
            f.parentNode.insertBefore(j, f);
        })(window, document, 'script', 'dataLayer', 'GTM-TQ6ZC4F');</script>
    <!-- End Google Tag Manager -->

    <!-- Global site tag (gtag.js) - Google Analytics - SK 210422 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-B9GP0K48SR"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag() {
            dataLayer.push(arguments);
        }
        gtag('js', new Date());

        gtag('config', 'G-B9GP0K48SR');
    </script>
    <!-- End Global site tag (gtag.js) -->

    <!-- Facebook Pixel Code - Sk 210422 -->
    <script>
        !function (f, b, e, v, n, t, s) {
            if (f.fbq)return;
            n = f.fbq = function () {
                n.callMethod ?
                    n.callMethod.apply(n, arguments) : n.queue.push(arguments)
            };
            if (!f._fbq) f._fbq = n;
            n.push = n;
            n.loaded = !0;
            n.version = '2.0';
            n.queue = [];
            t = b.createElement(e);
            t.async = !0;
            t.src = v;
            s = b.getElementsByTagName(e)[0];
            s.parentNode.insertBefore(t, s)
        }(window, document, 'script',
            'https://connect.facebook.net/en_US/fbevents.js');
        fbq('init', '468174144423257');
        fbq('track', 'PageView');
    </script>
    <noscript><img height="1" width="1" style="display:none"
                   src="https://www.facebook.com/tr?id=468174144423257&ev=PageView&noscript=1"
        /></noscript>
    <!-- End Facebook Pixel Code -->

    <title>🏆 La mejor academia Guardia Civil calidad/precio</title>
    <style type="text/css">
        @font-face {
            font-family: "Proxima Nova Regular";
            src: url("neostudio/pnswr.ttf");
        }

        @font-face {
            font-family: "Proxima Nova Bold";
            src: url("neostudio/pnsb.otf");
        }

        @font-face {
            font-family: "Proxima Nova Soft";
            src: url("neostudio/pss.ttf");
        }

        @font-face {
            font-family: "Rounded";
            src: url("neostudio/rou.ttf");
        }

        @font-face {
            font-family: "Regular";
            src: url("neostudio/pnswr.ttf");
        }

        @font-face {
            font-family: "Bold";
            src: url("neostudio/pnsb.otf");
        }

        b {
            font-family: Proxima Nova Bold;
        }

        body {
            font-family: Proxima Nova Regular;
            background: url('neostudio/bodyback.jpg');
            background-size: 100% 100%;
        }

        #tB:focus {
            outline: 1px;
        }

        .mU {
            list-style-type: none;
        }

        .liU:before {
            content: '-';
            position: absolute;
            margin-left: -20px;
        }

        a {
            color: #777;
        }

        a:hover {
            text-decoration: none;
            color: white;
        }

        #nav li {
            display: inline;
        }

        #main-container {
            width: 100%;
            margin-top: -32px;
        }

        #main-video {
            object-fit: fill;
            width: 100%;
            height: 100%;
        }

        .logoLeft {
            width: 30%;
            height: auto;
            position: absolute;
            left: 1px;
            top: 0px;
            z-index: 1;
        }

        .logoRight {
            display: none;
        }

        .firstLi {
            padding-right: 75px;
        }

        #sv {

        }

        .underVideoDiv {

        }

        .mainHeading {
            display: none;
            margin-left: 4%;
        }

        .underHeadingDiv {

        }

        .comienza {
            width: 100%;
            margin-left: 5%;
        }

        .content {
            display: none;
            margin-left: 5%;
            margin-right: 1%;
        }

        .underContentDiv {
            background: url(neostudio/4.png);
            background-size: contain;
            background-repeat: no-repeat;
        }

        .buttonsP {
            margin-left: 15%;
        }

        .button1 {
            width: 40%;
        }

        .button2 {
            width: 40%;
        }

        .footerImage {
            left: 0;
            bottom: 0;
            width: 20%;
        }

        .handClass {
            width: 100px;
            margin-top: 5px;
        }

        .cara {

        }

        #navbarSupportedContent {
            padding-left: 30%;
        }

        .forLg {
            display: none;
        }

        .forSm {
            display: block;
        }

        .compIm {
            width: 100%;
            margin: 0;
            position: absolute;
            top: 50%;
            left: 50%;
            -ms-transform: translate(-50%, -50%);
            transform: translate(-50%, -50%);
        }

        .botIm {
            width: 95%;
            margin: 0;
            position: absolute;
            top: 50%;
            left: 50%;
            -ms-transform: translate(-50%, -50%);
            transform: translate(-50%, -50%);
        }

        .size1 {
            font-size: 75%;
        }

        .size2 {
            font-size: 70%;
        }

        .mar {
            margin-left: 0%;
            margin-right: 0%;
        }

        .fo {
            font-size: x-small;
        }

        .total {
            margin: auto;
            cursor: zoom-in;
            border: 1px hsl(0, 0%, 90%);
            transition: background-color 300ms;
            background: #434343;
            color: #fff;
            padding: 10px
        }

        img.btn-package, img.btn-tenure, img.img-product {
            cursor: pointer;
            max-width: 60%;
            height: auto;
        }

        img.quantity {
            cursor: pointer;
            max-width: 15%;
            height: auto;
        }

        .coupen img {
            cursor: pointer;
            max-width: 70%;
            height: auto;
        }

        .btn-payment {
            max-width: 20%;
            cursor: pointer;
        }

        @media only screen and (min-width: 1000px) {
            body {
                background: url('neostudio/bodyback.jpg');
                background-size: 100% 100%;
            }

            a {
                color: #777;
            }

            a:hover {
                text-decoration: none;
                color: white;
            }

            #nav li {
                display: inline;
            }

            #main-container {
                width: 100%;
                margin-top: -32px;
            }

            #main-video {
                object-fit: fill;
                width: 100%;
                height: auto;
            }

            .comienza {
                width: 40%;
            }

            .logoLeft {
                width: 10%;
                height: auto;
                position: absolute;
                left: 1px;
                top: 0px;
                z-index: 1;
            }

            .logoRight {
                display: block;
                width: 9%;
                height: auto;
                position: absolute;
                right: 2%;
                top: 0px;
                z-index: 1;
            }

            .firstLi {

            }

            #horizontal-style {
                display: table;
                width: 70%;
                margin-left: 5%;
                table-layout: fixed;

            }

            #horizontal-style li {
                display: table-cell;
            }

            #horizontal-style a {
                display: block;

                text-align: center;
                margin: 0 0px;

            }

            #sv {
                background: url('neostudio/Header1.png');
                background-size: 100% 100%;
                padding-top: 2%;
                padding-bottom: 4%;
                border: 0
            }

            .underVideoDiv {
                margin-top: -150px;
                background: url(neostudio/4.png);
                background-size: contain;
                background-repeat: no-repeat;
            }

            .mainHeading {
                display: block;
                visibility: hidden;
                margin-left: 15%;
                padding-top: 5%;
            }

            .underHeadingDiv {
                margin-left: 20%;
                margin-right: 20%;
            }

            .content {
                display: block;
                visibility: hidden;
                margin-left: 0;
                margin-right: 0;
            }

            .underContentDiv {
                background: url();
            }

            .buttonsP {
                margin-left: 35%;
                padding-top: 5%;
            }

            .button1 {
                width: 20%;
            }

            .button2 {
                width: 20%;
            }

            .footerImage {
                left: 0;
                bottom: 0;
                width: 10%;
            }

            .handClass {
                width: 375px;
                margin-top: -100px;
            }

            #navbarSupportedContent {
                padding-left: 0;
            }

            .cara {
                margin-top: 0;
            }

            .forLg {
                display: block;
            }

            .forSm {
                display: none;
            }

            .compIm {
                width: 65%;
                margin: 0;
                position: absolute;
                top: 50%;
                left: 50%;
                -ms-transform: translate(-50%, -50%);
                transform: translate(-50%, -50%);
            }

            .botIm {
                width: 85%;
                margin: 0;
                position: absolute;
                top: 50%;
                left: 50%;
                -ms-transform: translate(-50%, -50%);
                transform: translate(-50%, -50%);
            }

            .size1 {
                font-size: 120%;
            }

            .size2 {
                font-size: 100%;
            }

            .mar {
                margin-left: 10%;
                margin-right: 10%;
            }

            .fo {
                font-size: small;
            }

            img.btn-package, img.img-product {
                max-width: 60%;
            }

            .btn-payment {
                max-width: 20%
            }

        }

        @media only screen and (max-width: 767px) {
            img.btn-package, img.btn-tenure, .coupen img, img.img-product {
                max-width: 100%;
            }

            .text-font {
                font-size: 12px;
            }

            img.quantity {
                max-width: 25%
            }

            .btn-payment {
                max-width: 80%
            }

            .btn-tenure-text {
                font-size: 11px;
            }

            .coupen-td {
                display: none;
            }
        }
    </style>
</head>

<body style="">
<!-- Google Tag Manager (noscript) - SK 210422 -->
<noscript>
    <iframe src="https://www.googletagmanager.com/ns.html?id=GTM-TQ6ZC4F"
            height="0" width="0" style="display:none;visibility:hidden"></iframe>
</noscript>
<!-- End Google Tag Manager (noscript) -->
@include('menus')

<br>
<div class="text-center"><h2 style="font-family: Proxima Nova Bold;">¡Comienza Ya!</h2></div>
<span class="forLg"></span>
@if (!empty($message))
    <div class="alert alert-success alert-dismissible">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <h5>Alerta!</h5>
        {{$message}}
    </div>
@endif
<div class="forSm container">
    <!--<img style="width: 100%;" src="neostudio/cab.png">-->
    @if(isset($top_video) && empty($top_video) === false)
        <div>
            <video controls playsinline id="main-video">
                <source src="{{asset($top_video->video)}}">
            </video>
        </div>
    @else
        <img style="width: 100%;" src="neostudio/ffc.png">
    @endif
</div>
<!--<div class="container forSm" class="wheelOuter" style="margin-top: -38px;">
  <img src="neostudio/upC.png" class="wheel" style="width: 87%; transform: translateX(11%); ">
</div>-->

<!--<div class="text-center">
  <p>Alumnos de años consecutivos solo <b>360 € (30 € / mes)</b></p>
</div>-->

<?php /*div class="forSm">
    <?php
    $p = \App\Amount::where('amountType', 'books')->first();
    if ($p) {
        $price = $p->amount;
    }
    $rand = rand(1, 7);
    $str = \App\Amount::where('amountType', 'strike')->first();
    if ($str) {
        $priceStr = $str->amount;
    }
    ?>
    <div style="margin-left: 2%; margin-right: 2%;">
        <div class="row">
            <div class="col-5">
                <div class="row">
                    <div class="col-3">
                        <img src="neostudio/bot1a.png" style="width: 75px;">
                    </div>
                    <div class="col-9 ml-1" style="position: absolute;top: 50%;transform: translate(50%,-50%);">
                        <span style="font-size: 90%;"><b>Temario</b> +<br> <b>Libro guía</b><!--<br><p style="line-height: 1; font-size: 75%;">Últimos cupones<br> del -25%: <b>{{$rand}}</b></p>--></span>
                    </div>
                </div>
            </div>
            <div class="col-4" style="transform: translateY(20%);">
                <p style="font-size: 100%; line-height: 1">
                    <span style="font-size: 75%;"><span style='color:red;text-decoration:line-through'>
                    <span style='color:black'>@if(!empty($priceStr)){{$priceStr}}@endif</span>
                    </span> €</span> <b>@if(!empty($price)){{$price}}@endif</b> €
                </p>
            </div>
            <div class="col-3" style="transform: translateY(30%);">
                @if(!empty($userId))
                    <a href="{{url('payment/'.$userId.'/'.'books')}}"><img src="neostudio/comp.png" style="width: 105px ; margin-left: -42px;"></a>
                @endif
                @if(empty($userId))
                    <a href="{{url('payment/'.'nul'.'/'.'books')}}"><img src="neostudio/comp.png" style="width: 105px ; margin-left: -42px;"></a>
                @endif
            </div>
        </div>
        <?php
        use Carbon\Carbon;
        $month = Carbon::now()->month;
        $ae = \App\Price::where('studentType', 'Alumno')->where('type', 'once')->exists();
        $aNowSE = \App\Price::where('studentType', 'Alumno')->where('type', 'recurring')->exists();
        if ($ae == true) {
            $a = \App\Price::where('studentType', 'Alumno')->where('type', 'once')->first();
            $amount = $a->amount;
        }
        if ($aNowSE == true) {
            $aNowS = \App\Price::where('studentType', 'Alumno')->where('type', 'recurring')->first();
            $aNowSA = $aNowS->amount;
        }
        ?>

        <div class="row">
            <div class="col-6">
                <div class="row">
                    <div class="col-3">
                        <img src="neostudio/bot2a.png" style="width: 75px;">
                    </div>
                    <div class="col-9" style="position: absolute;top: 70%;transform: translate(50%,-38%);">
                        <p style="font-size: 90%;line-height: 1"><b>Curso <!--online-->online</b>
                            <!--<br><p style="line-height: 1; font-size: 75%; margin-top: -15px;">(ciclo de 12 meses<br> hasta examen oficial)Acceso 10 meses<br> (49,8 €/mes)</p>-->
                        </p>
                    </div>
                </div>
            </div>

            <div class="col-3" style="transform: translateY(40%);">
                <p style="font-size: 100%; line-height: 1; margin-left: -25px;"><!--<span style="font-size: 65%;"><span style='color:red;text-decoration:none;'>
  <span style='color:black'>@if(!empty($aNowSA)){{$aNowSA}}@endif</span>
</span> €/mes</span><br>--><b>@if(!empty($aNowSA)){{$aNowSA}}@endif</b> €/mes</p>
            </div>
            <div class="col-3" style="transform: translateY(30%);">
                @if(!empty($userId))
                    <a href="{{url('payment/'.$userId.'/'.'alumno')}}"><img src="neostudio/comp.png" style="width: 105px ; margin-left: -42px;"></a>
                @endif

                @if(empty($userId))
                    <a href="{{url('payment/'.'nul'.'/'.'alumno')}}"><img src="neostudio/comp.png" style="width: 105px ; margin-left: -42px;"></a>
                @endif
            </div>

        </div>


    </div>
    <div style="margin-left:5%;margin-right:5%;">
        <p style="text-align: center;">Los precios incluyen el <b>IVA</b> y <b>gastos de envío</b>.</p>
    </div>
</div */?>
<!--For web screen start-->
<span class="forLg"></span>
<div class="forLg container">
    <!--<img style="width: 100%;" src="neostudio/cab.png">-->
    @if(isset($top_video) && empty($top_video) === false)
        <div>
            <video controls playsinline id="main-video">
                <source src="{{asset($top_video->video)}}">
            </video>
        </div>
    @else
        <img style="width: 100%;" src="neostudio/ffc.png">
    @endif

</div>
<!--<div class="container cara text-center forLg">
  <img src="neostudio/upC.png" class="comienza">

</div>-->

<div class="container" style="font-family: Proxima Nova Bold;">
    <div class="row mt-3 coupen">
        <div class="col-md-12 text-center">
            <p><strong>{{$texts['coupen_botones']}}</strong></p>
        </div>
        <div class="col-4 col-md-3 col-sm-2 coupen-td">&nbsp;</div>
        <div class="col-6 col-md-3 col-sm-2 text-center  p-0">
            <img src="{{ url('neostudio/pages/comienza/No-bw.png') }}"></div>
        <div class="col-6 col-md-3 col-sm-6 text-center  p-0">
            <img src="{{ url('neostudio/pages/comienza/Yes-bw.png') }}"></div>
        {{--<div class="col-md-4 mt-3 text-center">
            <input type="text" class="form-control" name="coupen" id="coupen" value="">
        </div>--}}
    </div>
    <form action="javascript:void(0);" method="post">
        <input type="hidden" id="_token" name="_token" value="{{csrf_token()}}">
        <input type="hidden" id="package_id" name="package_id" value="{{$package_data->id}}">
        <input type="hidden" name="number" id="number" value="recurring">
        <input type="hidden" name="packageTenure" id="packageTenure" value="fractional">
        <input type="hidden" name="userId" value="{{ (isset($user_data) && empty($user_data->id) === false) ? $user_data->id : '' }}">
        <div class="row mt-5 packages">
            @if($packages)
                <div class="col-md-12 text-center">
                    <p><strong>{{$texts['encima_de_los_paquetes']}}</strong></p>
                </div>
                @php $i=0; $images = array(); @endphp
                @foreach($packages As $package)
                    <div class="col-4 col-md-4 text-center p-1">
                        <h5 style="display: block;">{{ $package->title }}</h5>
                        <img src="{{ url(($package->default == 1) ? $package->image_active : $package->image ) }}" data-src="{{ url($package->image_active) }}" class="btn-package" id="btn-package-{{$i}}" data-cnt="{{$i}}" data-id="{{$package->id}}" data-price="{{$package->price}}">
                    </div>
                    @php $i++; $images[] = $package->image; @endphp
                @endforeach
                <div class="col-12 col-md-12 text-center mt-2">
                    <strong class="btn-tenure-text">{{$texts['botones_del_pacakges']}}</strong>
                </div>
                @php $images = json_encode($images); $cnt=0; @endphp
                @foreach($payment_buttons As $payment_button)
                    @php
                        $column = 'price_'.$price_types[$cnt];
                        $column_text = 'text_'.$price_types[$cnt];
                    @endphp
                    <div class="col-4 col-md-4 text-center p-0">
                        <img src="{{ url('neostudio/pages/comienza/'.$payment_button) }}" class="btn-tenure" id="btn-tenure-{{$cnt}}" data-cnt="{{$cnt}}" data-price="{{ $package_price->$column }}">
                        <p class="mt-2">
                            <span id="texts-{{$cnt}}">{{$package_price_text->$column_text}}</span><br/><span id="prices-{{$cnt}}">{!! $package_price->$column  !!} </span>
                        </p>
                    </div>
                    @php $cnt++; @endphp
                @endforeach
                <div class="col-12 col-md-12 text-center mt-2">
                    <strong class="btn-tenure-text">{{$texts['precios_totales']}}</strong>
                </div>
                <div class="col-12 col-md-12 text-center mt-4">
                    <p>
                        <strong class="total">Total:
                            <span class="total-package">{{ (float) (str_replace('€','',$package_price->price_annual))  * 2 }} &euro; ({{$pcakge_total_text[1]}})</span>
                            IVA Incl.
                        </strong>
                    </p>
                </div>
                <div class="col-12 col-md-12 text-center mt-2  p-0">
                    <img src="{{ url('neostudio/pages/comienza/Realizar-pedido.png') }}" class="btn-payment btn-package-payment" data-uid="{{ (isset($user_data) && empty($user_data->id) === false) ? $user_data->id : '' }}">
                </div>
            @endif
        </div>
    </form>
    @if(session()->has('error'))
        <div class="alert alert-danger mt-2 mb-2">
            {{ session()->get('error') }}
        </div>
    @endif
    <form action="javascript:void(0);" method="post">
        <input type="hidden" id="_token" name="_token" value="{{csrf_token()}}">
        <input type="hidden" id="total_quantity" name="total_quantity" value="0">
        <input type="hidden" id="total_price" name="total_price" value="0">
        <input type="hidden" id="shipping" name="shipping" value="{{ (isset($shipping) && empty($shipping->price) === false) ? $shipping->price : 0 }}">
        <input type="hidden" name="userId" value="{{ (isset($user_data) && empty($user_data->id) === false) ? $user_data->id : '' }}">
        <div class="products mt-5">
            @if($products)
                @php $cnt=0; @endphp
                @foreach($products As $product)
                    <div class="row mt-3">
                        <div class="col-3 col-md-4 text-center p-0">
                            <img src="{{ url($product->photo) }}" class="img-product">
                        </div>
                        <div class="col-5 col-md-5 text-left">
                            <h5 class="text-font">{{ $product->name }}</h5>
                        </div>
                        <div class="col-4 col-md-3 text-center p-0">
                            <span class="text-font">Cantidad</span><br/>
                            <img src="{{ url('neostudio/pages/comienza/Signo-menos.png') }}" class="quantity quantityDec" data-pid="{{$product->id}}" data-price="{{$product->price}}">
                            <span class="quantity-text ml-2 mr-2" id="quantity-{{$product->id}}">0</span>
                            <input type="hidden" name="pids[{{$product->id}}]" id="pid-{{$product->id}}" value="">
                            <img src="{{ url('neostudio/pages/comienza/Signo-mas.png') }}" class="quantity quantityInc" data-pid="{{$product->id}}" data-price="{{$product->price}}">
                            <br/><span class="text-font">{{ (fmod($product->price, 1) !== 0.00) ? $product->price : round($product->price)}}&euro;</span>
                        </div>
                    </div>
                @endforeach
                @if($shipping)
                    <div class="row mt-3">
                        <div class="col-3 col-md-4 text-center  p-0">
                            <img src="{{ url($shipping->image) }}" class="img-product">
                        </div>
                        <div class="col-5 col-md-5 text-left">
                            <h5 class="text-font">{{ $shipping->title }}</h5>
                        </div>
                        <div class="col-4 col-md-3 text-center">
                            <br/><span class="text-font">Priceo<br/>{{ (fmod($shipping->price, 1) !== 0.00) ? $shipping->price : round($shipping->price)}}&euro;</span>
                        </div>
                    </div>
                @endif
                <div class="col-12 col-md-12 text-center mt-4">
                    <p><strong class="total">Total: <span class="total-product">0&euro;</span></strong></p>
                </div>
                <div class="col-12 col-md-12 text-center mt-2 p-0">
                    <img src="{{ url('neostudio/pages/comienza/Realizar-pedido.png') }}" class="btn-payment btn-product-payment" data-uid="{{ (isset($user_data) && empty($user_data->id) === false) ? $user_data->id : '' }}">
                </div>
            @endif
        </div>
    </form>
</div>
<?php /*div class="forLg">
    <span class="forLg"><br><br><br><br><br><br><br><br></span>

    <div class="row mar" style="">

        <div class="col-md-3 text-center" style="position: relative;">
            <img class="botIm" src="neostudio/bot1a.png" style="">
        </div>
        <?php
        $p = \App\Amount::where('amountType', 'books')->first();
        if ($p) {
            $price = $p->amount;
        }
        $rand = rand(1, 7);
        $str = \App\Amount::where('amountType', 'strike')->first();
        if ($str) {
            $priceStr = $str->amount;
        }
        ?>
        <div class="col-md-4" style="position: relative;">
            <p class="size1" style="margin: 0;position: absolute;top: 50%;left: 50%;
      -ms-transform: translate(-50%, -50%);transform: translate(-50%, -50%);"><b>Temario</b> +<br> <b>Libro guía</b><br><span class="size2" style="visibility: hidden;">Últimos cupones del -25%: <b>{{$rand}}</b></span>
            </p>
        </div>

        <div class="col-md-2 text-center" style="position: relative;">
            <p style="font-size: 120%; margin: 0;position: absolute;top: 50%;left: 50%;-ms-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%);"><span style="font-size: 75%;"><span style='color:red;text-decoration:line-through'>
  <span style='color:black'>@if(!empty($priceStr)){{$priceStr}}@endif</span>
</span> €</span><br><b>@if(!empty($price)){{$price}}@endif</b> €</p>
        </div>

        <div class="col-md-3 col-3 text-center" style="position: relative;">
            @if(!empty($userId))
                <a href="{{url('payment/'.$userId.'/'.'books')}}"><img class="compIm" src="neostudio/comp.png" style=""></a>
            @endif
            @if(empty($userId))

                <a href="{{url('payment/'.'nul'.'/'.'books')}}"><img class="compIm" src="neostudio/comp.png" style=""></a>
            @endif
        </div>

    </div>
    <span class="forLg"><br><br><br><br><br><br><br></span>

    <div class="row mar" style="">

        <div class="col-md-3 text-center" style="position: relative;">
            <img class="botIm" src="neostudio/bot2a.png" style="">
        </div>

        <div class="col-md-4" style="position: relative;">
            <p class="size1" style="margin: 0;position: absolute;top: 50%;left: 35%;
      -ms-transform: translate(-50%, -50%);transform: translate(-50%, -50%);text-align: left;"><b>Curso <!--online-->
                    online</b>
                <!--<br><span class="size2" style="visibility: hidden;">(Ciclo de 12 meses hasta examen oficial) Acceso 10 meses (49,8 €/mes)</span>-->
            </p>
        </div>
        <?php

        $month = Carbon::now()->month;


        $ae = \App\Price::where('studentType', 'Alumno')->where('type', 'once')->exists();
        $aNowLE = \App\Price::where('studentType', 'Alumno')->where('type', 'recurring')->exists();
        if ($ae == true) {
            $a = \App\Price::where('studentType', 'Alumno')->where('type', 'once')->first();
            $amount = $a->amount;
        }
        if ($aNowLE == true) {
            $aNowL = \App\Price::where('studentType', 'Alumno')->where('type', 'recurring')->first();
            $aNowLA = $aNowL->amount;
        }
        ?>
        <div class="col-md-2 text-center" style="position: relative;">
            <p style="font-size: 120%; margin: 0;position: absolute;top: 50%;left: 40%;-ms-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%);"><!--<span style="font-size: 70%;"><span style='color:red;text-decoration:none'>
  <span style='color:black'>@if(!empty($aNowLA)){{$aNowLA}}@endif</span>
</span> €/mes</span><br>--><b>@if(!empty($aNowLA)){{$aNowLA}}@endif</b> €/mes</p>
        </div>

        <div class="col-md-3 col-3 text-center" style="position: relative;">
            @if(!empty($userId))
                <a href="{{url('payment/'.$userId.'/'.'alumno')}}"><img class="compIm" src="neostudio/comp.png" style=""></a>
            @endif
            @if(empty($userId))

                <a href="{{url('payment/'.'nul'.'/'.'alumno')}}"><img class="compIm" src="neostudio/comp.png" style=""></a>
                -->
            @endif
        </div>

    </div>

    <span class="forLg"><br><br><br><br><br><br><br><br><br></span>

    <div style="margin-left:5%;margin-right:5%;">
        <!--<p style="text-align: center;">Los precios incluyen el <b>IVA</b> y <b>gastos de envío</b>.</p>-->
        <p style="text-align: center;">Se incluye el <b>IVA</b> y <b>gastos de envío (península)</b>.</p>
    </div>
</div */?>
<!--For web screen end-->
<div class="underVideoDiv mt-5">
    <!--<br><br><br>-->

    @if(isset($contents) && empty($contents) === false)
        <div class="container ssp2">
            @foreach($contents AS $content)
                @if(isset($content->image) && empty($content->image) === false)
                    <p style="text-align: center;">
                        @if(isset($isMobile) && $isMobile == true)
                            <img src="{{asset(str_replace('thumbs/','',$content->image))}}" alt="{{$content->title}}" class="img-fluid">
                        @else
                            <img src="{{asset(str_replace('thumbs/','',$content->image))}}" class="img-fluid">
                        @endif
                    </p>
                @endif
                @if(isset($content->description2) && empty($content->description2) === false)
                    {!! $content->description2 !!}
                @endif
                @if(isset($content->video) && empty($content->video) === false)
                    <div>
                        <video controls playsinline id="main-video">
                            <source src="{{asset($content->video)}}">
                        </video>
                    </div>
                @endif
            @endforeach
        </div>
@endif

<!--<br><br><br><br>-->
    <div class="underContentDiv">
        <p class="buttonsP">
            <a style="text-decoration: none;" href="https://apps.apple.com/es/app/neoestudio-guardia-civil/id1531939360"><img class="button1" src="neostudio/ddd2.png"></a>&nbsp;&nbsp;&nbsp;<a href="https://play.google.com/store/apps/details?id=com.neostudio&hl=es_419" style="text-decoration: none;"><img class="button2" src="neostudio/ddd.png"></a>
        </p>
        <p class="forSm" style="text-align: center; margin-bottom: -5%; margin-top: 20%; font-size: small;color: grey;">
            © 2020 Neoestudio Academia Online S.L.<br class="forSm">Todos los derechos reservados <br class="forSm">
        </p>
        <p class="forLg" style="text-align: center; margin-bottom: -5%; margin-top: 20%; font-size: small;color: grey;">
            © 2020 Neoestudio Academia Online S.L. Todos los derechos
            reservados.<br><a style="color: black;" href="{{url('aviso-legal-y-términos-de-uso')}}"> Aviso legal</a>
            -
            <a href="{{url('política-de-cookies')}}" style="color: black;">Política de Cookies</a> -
            <a style="color: black;" href="{{url('política-de-privacidad-y-protección-de-datos')}}">Política de
                Privacidad</a></p>
        <img class="footerImage" src="neostudio/5.png">

    </div>
</div>


</body>
<script type="text/javascript">
    // Get media - with autoplay disabled (audio or video)
    var media = $('video').not("[autoplay='autoplay']");
    var tolerancePixel = 40;

    function checkMedia() {
        console.log('method');
        // Get current browser top and bottom
        var scrollTop = $(window).scrollTop() + tolerancePixel;
        var scrollBottom = $(window).scrollTop() + $(window).height() - tolerancePixel;


        media.each(function (index, el) {
            var yTopMedia = $(this).offset().top;
            var yBottomMedia = $(this).height() + yTopMedia;
            if (scrollTop < yBottomMedia && scrollBottom > yTopMedia) { //view explaination in `In brief` section above
                var element_position = $('.ssp2').offset().top;
                var y_scroll_pos = window.pageYOffset;
                var scroll_pos_test = element_position;
                console.log(y_scroll_pos, scroll_pos_test);
                if (y_scroll_pos < scroll_pos_test) {
                    $(this).get(0).play();
                    console.log('in');
                }
            } else {
                $(this).get(0).pause();
                console.log('out');
            }
        });

        //}
    }

    $(document).on('scroll', checkMedia);
    $(document).ready(function () {
        $.ajaxSetup({
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            }
        });

        console.log("ready!");
        setTimeout(function () {
            $('#ww').css('display', 'block');
        }, 6000);
    });
    $(document).on('click', '.btn-package-payment', function () {
        var ppActive = $(this).parents('form').find('input[name="package_id"]').val();
        var user_id = $(this).attr('data-uid');
        if (typeof ppActive === "undefined" || ppActive == "" || ppActive == 0) {
            alert('¡Elija cualquier paquete haciendo clic en la imagen del paquete!');
            return false;
        }

        if (typeof user_id == "undefined" || user_id == "" || user_id <= 0) {
            window.location.href = 'registerate';
        }
        else {
            $(this).parents('form').attr('action', 'temario').submit();
        }
    });
    $(document).on('click', '.btn-tenure', function () {
        var imageIndex = $(this).attr('data-cnt');
        var price = $(this).attr('data-price');
        var images = ['Anual', 'Fraccionado', 'Mensual'];
        var prices = ['annual', 'fractional', 'monthly'];
        var meses = ['12 meses', '6 meses', '1 mes'];
        if (images.length > 0) {
            $.each(images, function (k, v) {
                $('#btn-tenure-' + k).attr('src', 'neostudio/pages/comienza/' + v + '-bw.png')
            })
        }
        var package_id = $(this).parents('form').find('input[name="package_id"]').val();
        $('#btn-tenure-' + imageIndex).attr('src', 'neostudio/pages/comienza/' + images[imageIndex] + '.png');
        $(this).parents('form').find('input[name="packageTenure"]').val(prices[imageIndex].toLocaleLowerCase());
        var price_type = $(this).parents('form').find('input[name="packageTenure"]').val();
        $.ajax({
            url: "{{ url('get_package_data')}}",
            method: 'post',
            data: {'id': package_id, 'price_type': price_type},
            success: function (result) {
                var data = JSON.parse(result);
                $.each(data, function (k, v) {
                    if(k == 2){
                        $('#prices-' + k).html(v);
                    }
                    else{
                        $('#prices-' + k).html(v+' €');
                    }
                });
                var totalP = data[imageIndex];
                if(imageIndex == 0){
                    totalP = data[1];
                }
                if(imageIndex == 1){
                    totalP = (data[0] * 2);
                }
                if(imageIndex == 2){
                    totalP = data[0];
                }
                $('.total-package').html(totalP + ' &euro; (' + meses[imageIndex].toLocaleLowerCase() + ')');
            }
        });
        $.ajax({
            url: "{{ url('get_package_text')}}",
            method: 'post',
            data: {'id': package_id, 'price_type': price_type},
            success: function (result) {
                var data = JSON.parse(result);
                $.each(data, function (k, v) {
                    $('#texts-' + k).html(v);
                });
            }
        });
    });

    $(document).on('click', '.btn-package', function () {
        var package_id = $(this).attr('data-id');
        var price_type = $(this).parents('form').find('input[name="packageTenure"]').val();
        var active_imge = $(this).attr('data-src');
        var price = $(this).attr('data-price');
        var imageIndex = $(this).attr('data-cnt');
        var images = JSON.parse('<?=$images;?>');
        var meses = ['12 meses', '6 meses', '1 mes'];
        var ppActive = $(this).parents('form').find('input[name="package_id"]').val();
        if (images.length > 0) {
            $.each(images, function (k, v) {
                $('#btn-package-' + k).attr('src', v)
            })
        }
        $.ajax({
            url: "{{ url('get_package_data')}}",
            method: 'post',
            data: {'id': package_id, 'price_type': price_type},
            success: function (result) {
                var data = JSON.parse(result);
                $.each(data, function (k, v) {
                    if(k == 2){
                        $('#prices-' + k).html(v);
                    }
                    else{
                        $('#prices-' + k).html(v+' €');
                    }
                });
                var priceIndex = 0;
                var totalP = data[imageIndex];
                if (price_type == "annual") {
                    priceIndex = 0;
                    totalP = data[1];
                }
                if (price_type == "fractional") {
                    priceIndex = 1;
                    totalP = (data[0] * 2);
                }
                if (price_type == "monthly") {
                    priceIndex = 2;
                    totalP = data[0];
                }
                $('.total-package').html(totalP + ' &euro; (' + meses[priceIndex].toLocaleLowerCase() + ')');
            }
        });
        $.ajax({
            url: "{{ url('get_package_text')}}",
            method: 'post',
            data: {'id': package_id, 'price_type': price_type},
            success: function (result) {
                var data = JSON.parse(result);
                $.each(data, function (k, v) {
                    $('#texts-' + k).html(v);
                });
            }
        });
        $('#btn-package-' + imageIndex).attr('src', active_imge);
        $(this).parents('form').find('input[name="package_id"]').val(package_id);
    });

    $(document).on('click', '.btn-product-payment', function () {
        var total_quantity = $(this).parents('form').find('input[name="total_quantity"]').val();
        var user_id = $(this).attr('data-uid');
        if (typeof total_quantity === "undefined" || total_quantity == "" || total_quantity == 0) {
            alert('Al menos una cantidad de producto puede ser mayor que cero para proceder con el pago, así que haga clic en el signo más de cualquier producto.');
            return false;
        }

        if (typeof user_id == "undefined" || user_id == "" || user_id <= 0) {
            window.location.href = 'registerate';
        }
        else {
            $(this).parents('form').attr('action', 'coursePay').submit();
        }
    });

    $(document).on('click', '.quantityInc', function () {
        var product_id = $(this).attr('data-pid');
        var price = $(this).attr('data-price');
        var total_quantity = parseInt($(this).parents('form').find('input[name="total_quantity"]').val());
        var total_price = parseFloat($(this).parents('form').find('input[name="total_price"]').val());
        var shipping = $(this).parents('form').find('input[name="shipping"]').val();
        var product_quantity = parseInt($('#quantity-' + product_id).html());
        var productInc = (product_quantity + 1);
        var totalQuantity = (total_quantity + 1);
        console.log(price);
        var totalPrice = parseFloat(price) + parseFloat(shipping);
        if (total_price > 0) {
            totalPrice = (total_price + parseFloat(price));
        }
        $('#quantity-' + product_id).html(productInc);
        $(this).parents('form').find('input[name="total_quantity"]').val(totalQuantity);
        $(this).parents('form').find('input[name="total_price"]').val((totalPrice).toFixed(2));
        $('.total-product').html((totalPrice).toFixed(2) + '&euro;');
        $('#pid-'+product_id).val(productInc);
    });

    $(document).on('click', '.quantityDec', function () {
        var product_id = $(this).attr('data-pid');
        var price = $(this).attr('data-price');
        var total_quantity = parseInt($(this).parents('form').find('input[name="total_quantity"]').val());
        var total_price = parseFloat($(this).parents('form').find('input[name="total_price"]').val());
        var product_quantity = parseInt($('#quantity-' + product_id).html());
        var productInc = product_quantity - 1;
        var totalQuantity = total_quantity - 1;
        var totalPrice = 0;
        if (total_price > 0) {
            totalPrice = (total_price - parseFloat(price));
        }
        if (totalQuantity == 0) {
            $('.total-product').html('0&euro;');
            $(this).parents('form').find('input[name="total_price"]').val(0);
        }
        if (productInc < 0) {
            return false;
        }
        $('#quantity-' + product_id).html(productInc);
        $(this).parents('form').find('input[name="total_quantity"]').val(totalQuantity);
        if (totalQuantity > 0) {
            $(this).parents('form').find('input[name="total_price"]').val((totalPrice).toFixed(2));
            $('.total-product').html(parseFloat((totalPrice).toFixed(2)) + '&euro;');
        }
    });
</script>
</html>